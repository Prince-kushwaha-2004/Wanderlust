
<nav class="navbar navbar-expand-md sticky-top main-navbar">
    <div class="container-fluid px-md-5">
      <a class="navbar-brand d-flex align-items-center" href="/">
        <span class="brand-text">StayEasy<span class="brand-dot">.</span></span>
      </a>
      <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav ms-3">
          <a class="nav-link px-3" href="/listings"><i class="fa-solid fa-compass me-1"></i> Explore</a>
        </div>
        <div class="navbar-nav navbar-search-container my-2 my-md-0">
          <form class="d-flex align-items-center search-form shadow-sm" action="/listings" method="GET">
            <span class="d-flex align-items-center text-muted ps-2"><i class="fa-solid fa-location-dot text-primary-color fs-7"></i></span>
            <input class="search-input ps-2" type="search" name="search" placeholder="Search destination, country..." value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>">
            <button class="btn btn-search border-0" type="submit"><i class="fa-solid fa-magnifying-glass"></i></button>
          </form>
        </div>
        <div class="navbar-nav ms-auto align-items-md-center gap-2">
          <a class="nav-link host-badge px-3 py-1" href="/listings/new">Host your home</a>
          <% if(!currentUser){ %>
          <a class="nav-link auth-link px-3" href="/login">Login</a>
          <% } %>
          <% if(currentUser){ %>
          <div class="nav-item dropdown ms-md-2">
            <a class="nav-link dropdown-toggle d-flex align-items-center gap-1" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="fa-solid fa-circle-user profile-icon fs-4 text-muted"></i>
              <span class="d-md-inline d-none fw-semibold"><%= currentUser.username %></span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end border-0 shadow mt-2">
              <li><span class="dropdown-item-text text-muted fs-7">Welcome, <%= currentUser.username %>!</span></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item text-danger" href="/logout"><i class="fa-solid fa-right-from-bracket me-2"></i>Logout</a></li>
            </ul>
          </div>
          <% } %>
        </div>
      </div>
    </div>
  </nav> 